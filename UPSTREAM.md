# 上游来源与归属说明

NovelX Desktop 是基于 [OpenCode](https://github.com/anomalyco/opencode) 的独立衍生项目。本仓库当前历史基线来自 OpenCode `v1.18.3`，随后增加了 NovelX 的中文桌面界面、独立运行档案、Growth（生长）编排与小说创作领域能力。

## 归属与许可

- OpenCode 的原始源码与历史归其原作者和贡献者所有。
- 本仓库继续遵守根目录 [LICENSE](LICENSE) 中的 MIT License（MIT 许可证），并保留原始版权声明。
- NovelX Desktop 与 OpenCode 项目及其维护团队没有隶属、赞助、认证或背书关系。
- `OpenCode` 名称仅用于说明上游来源与 Runtime（运行时）兼容关系，不代表 NovelX 的产品名称。

## 为什么代码中仍有 `opencode`

当前仍保留部分上游包名、内部路径、协议名和构建元数据。它们属于兼容性边界；在发布协议、自动更新和数据迁移尚未完成专项审查前，贸然全量重命名可能破坏现有会话、插件或构建链。

因此，本次公开品牌修正只改变仓库首页与项目说明，不把内部标识重命名伪装成“已经完全脱离上游”。后续若修改这些边界，应单独评审兼容性并提供迁移方案。

## 上游同步

本项目维护工作区约定将原项目配置为只读 `upstream` 远端，将 NovelX 公开仓库配置为 `origin`；新克隆不会自动继承这组远端名称。上游更新应先经过差异、协议与数据兼容性审查，再合入 NovelX 开发分支。
