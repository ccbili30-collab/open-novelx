# NovelX 项目切换栏与当前项目侧栏实施记录

## 实际完成

- 在新布局常驻层注册 `project.open`，项目窄栏 `+`、命令面板和 `Ctrl+O` 共享同一命令。
- 目录选择取消时零副作用；选择多个目录时全部登记，只激活第一个目录的新会话草稿。
- 项目窄栏移除彩色 `ProjectIcon`，改为项目名称首字；中文保留首字，拉丁字母大写，空名称回退为 `?`。
- 展开导航改为 60px 项目窄栏与 300px 当前项目侧栏并列。
- 当前项目侧栏不再循环渲染全部项目，只显示当前项目名称、置顶/新会话操作、置顶快捷方式及当前项目会话。
- 当前项目识别同时覆盖项目根目录与其沙盒目录；项目和会话拖动仍分别受原有边界约束。

## 验收证据

- 定向单元测试：8 项通过，17 次断言。
- TypeScript 类型检查：`bun run typecheck` 通过。
- 定向 Oxlint：0 警告、0 错误。
- 生产构建：Vite 转换 2578 个模块并成功产出。
- 真实 Electron（桌面壳）运行：
  - 运行时命令表包含 `project.open`；
  - 点击真实项目 `+` 后 Windows 原生“打开项目”目录框实际出现，随后取消；
  - 展开态实测总宽 360px、项目栏 60px、侧栏 300px；
  - 展开侧栏中旧项目分组数量为 0，当前项目会话数量为 5；
  - 运行截图覆盖到 `C:\Users\16014\Desktop\NovelX-Bugfix-Project-Sidebar.png`。

## 未扩大范围

- 未修改 Growth、世界内容、故事或图片生成链路。
- 未更改 Provider（模型服务）配置、协议、数据库或持久化格式。
- 未把项目封面重新接入项目方块；本批次按产品决定固定使用名称首字。

## 已知边界

- 为避免污染用户的项目集合，真实桌面验收在原生目录框出现后选择“取消”；“选择结果登记全部目录、只激活第一个目录”的分支由纯命令测试覆盖，尚未用临时项目写入用户的持久项目列表。
- Windows 工作树将 `packages/app/src/custom-elements.d.ts` 的 Git 符号链接检出为路径文本。类型检查时临时展开真实声明并在检查后恢复；该环境问题未混入提交。
- 本批次运行的是定向测试、类型检查和前端生产构建，不代表仓库全量测试或完整 NovelX 产品闭环。
