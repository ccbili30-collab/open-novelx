# NovelX Growth 与 Bugfix 联合集成状态

日期：2026-07-22

分支：`codex/novelx-integration`

基线：`codex/novelx-bugfix` 的 `be7da26bf`

## 本批已完成

- 保留 Bugfix 线的桌面外壳、项目与会话操作、文件工作面、公开流式正文、写入锁、图谱、品牌、独立 Profile、Electron 与安装包代码。
- 选择性合入 Content Growth 线已经验证的“冻结世界 → 唯一主角 → 一部三章小说”文字链；小说必须绑定正式主角，并且每章至少绑定一个冻结世界实体及其原文哈希。
- Story V1 已完成项目保持可读、可验证和终态，不追溯创建角色；未完成 Story V1 明确失败关闭，本批不承诺迁移。
- 建立角色标准立绘合同、队列和工具注册；立绘绑定角色物化完整性、正式角色档案和世界来源。
- 恢复并统一世界地图/风貌、角色立绘、小说封面的非阻塞视觉路线。视觉任务登记后即继续文字链，图片排队、生成或失败都不会阻止文字链完成。
- 图志与纪行绑定不可变的世界视觉注册指纹，不再因为图片 Worker 的状态变化而失效；图志与纪行不等待图片返回。
- Growth 权威路由现在按以下顺序运行：

```text
世界事实封存
→ 登记地图/风貌并异步入队
→ 生成图志与纪行
→ 唯一主角档案
→ 登记主角立绘并异步入队
→ 一部三章小说
→ 登记小说封面与主题封面并异步入队
→ 文字链完成
```

- 桌面角色面板读取真实角色物化与立绘清单，校验完整性和来源绑定，并区分未注册、生成中、已挂载、文件缺失与失败状态。
- 用户可见对话继续执行防泄漏规则：不展示内部 Prompt、Agent 名称、工具名、哈希、会话 ID、注册过程或生产报告。
- 普通 OpenCode Session（会话）的重试行为没有修改；Content Growth 线的全局循环退避改动没有进入联合分支。

## 提交

- `483be1209`：合入正式人物、三章小说、世界来源约束与 Growth 状态栏。
- `b449c7b50`：合入非阻塞世界/角色/故事视觉合同、队列、路由与出版绑定。
- `b9106d426`：接入角色与立绘的桌面状态投影。

## 验收证据

- `packages/schema`: `bun run typecheck` 通过。
- `packages/opencode`: `bun run typecheck` 通过。
- `packages/app`: `bun run typecheck` 通过。
- OpenCode 定向回归：8 个文件，88 项测试通过，0 失败；覆盖 Agent 权限、工具注册、世界视觉、图志纪行、角色立绘、故事封面、人物与 Growth 路由。
- App 定向回归：12 个文件，58 项测试通过，0 失败；覆盖项目打开、文件过滤、世界面板、角色投影、图谱监听与布局、公开 Growth 状态。
- Chromium 工作台 E2E（端到端测试）：`novelx-workspace.spec.ts` 1 项通过。
- 本批没有使用真实 Provider（模型服务），也没有把 Fixture（测试夹具）结果标记为 Live（真实运行）。
- 本批没有运行全仓库全量测试、Electron 安装流程或安装包重建。

## 未完成与冻结边界

- 联合头尚未重新执行一次真实 Provider 的“一句话完整生成”验收，因此不得宣称联合版本已经 Live 跑通世界、图志纪行、主角、三章小说和所有视觉任务。
- 当前注册动作会启动异步图片 Worker；应用进程退出后，尚未完成的图片队列不会由只读 Growth 路由自动重启。需要补一个 Growth 专属后台恢复器，不能借此修改普通 OpenCode 重试语义。
- 角色和小说视觉会自动尝试至合同上限并保存失败状态；桌面端已经显示失败，但尚未提供用户点击的终态重新入队入口。这个入口必须显式重置指定视觉任务并保留审计，不能用重新生成 Prompt 代替。
- 本批不实现世界包，不提交离线世界包原型，也不合入 README/仓库主页改造。
- 人物立绘与小说封面仍需在联合头使用真实图片 Provider 验证实际挂载；当前证据只覆盖合同、状态机、HTTP 传输与 UI 投影。

## 后续恢复入口

1. 在 `codex/novelx-integration` 上增加 Growth 专属图片队列恢复器和显式失败重试入口。
2. 使用 NovelX 独立 Profile 的真实 Provider 配置，从新项目运行一次完整 `/growth`。
3. 核对文字链完成不等待图片、图片成功自动挂回、图片失败可独立重试。
4. 联合 Live 验收成立后，再从该联合基线实现世界包公开展示。
